import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF673AB7);
  static const accent = Color(0xFF1976D2);
  static const lightBlue = Color(0xFF40C4FF);
  static const danger = Color(0xFFD32F2F);
  static const background = Colors.white;
  static const card = Color(0xFFF5F5F5);
  static const text = Colors.black;
  static const subtitle = Color(0xFF757575);
  static const success = Color(0xFF388E3C);
  static const border = Color(0xFFE0E0E0);
  static const grey = Color(0xFF9E9E9E);

  // Dashboard specific colors
  static const dashboardBackground = Colors.white;
  static const dashboardAppBarText = Colors.white;
  static const dashboardTabIndicator = Colors.white;
  static const dashboardTabLabel = Colors.white;
  static const dashboardTabUnselectedLabel = Colors.white70;

  // Status indicator colors
  static const statusError = Colors.red;
  static const statusLive = Colors.green;
  static const statusConnecting = Colors.orange;
  static const statusText = Colors.white;

  // Error display colors
  static const errorBackground = Color(0xFFFFEBEE); 
  static const errorBorder = Color(0xFFFFCDD2); 

  // Dialog colors
  static const dialogBackground = Colors.white;
  static const dialogWarningIcon = Colors.red;
  static const dialogCancelButton = Color(0xFF424242); 
  static const dialogCancelBorder = Color(0xFFBDBDBD);

  static const dialogDeleteText = Colors.white;

  // Snackbar colors
  static const snackbarSuccess = Colors.green;
  static const snackbarError = Colors.red;
  static const snackbarInfo = Colors.blue;
}
